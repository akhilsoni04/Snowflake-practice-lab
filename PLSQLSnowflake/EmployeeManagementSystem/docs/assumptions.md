# Assumptions – Employee Management System

- Oracle Database is used.
- User has permission to create tables, sequences, triggers, packages, and indexes.
- A valid department must be created before hiring employees.
- Salary history is only maintained for salary updates (not for initial salary insert).
- Termination is implemented using STATUS column (logical termination).
